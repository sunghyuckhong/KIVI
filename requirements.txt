# Eval-harness deps that aren't already pulled in by the vllm fork install.
#
# Everything else (torch, transformers, tokenizers, datasets, etc.) comes
# transitively from `pip install -e $VLLM_FORK_PATH`. Pinning torch /
# transformers here would just fight that — leave them unpinned.
#
# The deps below are needed at eval time:
#   lm-eval     — the eval harness invoked by run_eval_vllm.py
#   math-verify — sympy-based boxed-aware verifier used by minerva_math500
#                 in scripts/scoring.py (and in adaptive_pass2 dispatch)
#   ray         — required by lm_eval.models.vllm_causallms.VLLM (it imports
#                 ray at module top-level even when TP=1 / no Ray runtime)

lm-eval>=0.4.0
math-verify>=0.5.0
ray>=2.50.0
# lm_eval/tasks/minerva_math/utils.py asserts version("antlr4-python3-runtime")
# starts with "4.11" at module load. latex2sympy2_extended (transitive dep
# of math-verify) wants >=4.13 but actually works with 4.11; pin to satisfy
# the lm-eval assertion.
antlr4-python3-runtime~=4.11.0
